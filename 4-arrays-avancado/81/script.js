// o que já foi visto
// const nomes2 = new Array('a', 'b', 'c'); // array com construtor
//delete nomes[1];
// const nomes = ['a', 'b', 'c']; // array literal
// const novo = nomes.slice(1, -1)
// console.log(novo);

const nomes = [ 'Valentina', 'Prado', '123' ];
const nome = nomes.join(' ');
console.log(nome);



//console.log(nomes.length); // tamanho
//nomes.push('d');
//const removido = nomes.pop(); // remove o último
//const removido = nomes.shift(); // remove o primeiro
// const novo = [...nomes];
// //nomes[2] = 'd';
// novo.pop();
// console.log(nomes);
// console.log(novo);